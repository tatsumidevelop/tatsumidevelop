
d:
cd /xampp/htdocs/hac
OR
cd /laravel-proj/project

TERMINAL
php artisan serve

form で GETを使う場合　他の変数を更新させないため
from で POSTい　roule(name, $post) -> 
	Route::post(url, 処理) 処理内で$postで値参照
from で POSTい　roule(name) ->
	Route::post(url, 処理の引数として$requestで値参照

// 変数
Log::debug($test);
// 配列
Log::debug(print_r($arr, true));
// オブジェクト
Log::debug(print_r($object, true));

用語説明
https://urashita.com/archives/7249
